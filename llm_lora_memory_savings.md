# LLM使用LoRA的显存节省分析

## 一、当前情况

**当前配置**:
- LLM参数量: **3.8B** (Phi-3-mini)
- 训练方式: **全量微调** (所有参数可训练)
- 显存占用: 3.8B × 2 bytes (BF16) = **7.6 GB** (参数)
- 优化器状态: 3.8B × 8 bytes (AdamW) = **30.4 GB**
- 梯度: 3.8B × 2 bytes = **7.6 GB**
- **LLM相关总显存**: ~45.6 GB

## 二、使用LoRA后的显存节省

### 2.1 LoRA参数量计算

**Phi-3-mini架构**:
- 层数: 32层
- 隐藏维度: 3072
- 中间维度: 8192

**LoRA配置** (r=16, 应用到attention和MLP):
- `self_attn.qkv_proj`: 3072 × 3072 × 3 = 28.3M → LoRA: 3072 × 16 × 2 = 0.098M
- `self_attn.o_proj`: 3072 × 3072 = 9.4M → LoRA: 3072 × 16 × 2 = 0.098M
- `mlp.gate_up_proj`: 3072 × 8192 × 2 = 50.3M → LoRA: 3072 × 16 × 2 + 8192 × 16 × 2 = 0.36M
- `mlp.down_proj`: 8192 × 3072 = 25.2M → LoRA: 8192 × 16 × 2 = 0.26M

**每层LoRA参数量**: ~0.82M
**32层总LoRA参数量**: ~26.2M (约0.026B)

### 2.2 显存节省对比

**使用LoRA后**:
- LoRA参数: 0.026B × 2 bytes = **0.052 GB**
- 优化器状态: 0.026B × 8 bytes = **0.208 GB**
- 梯度: 0.026B × 2 bytes = **0.052 GB**
- **LoRA相关总显存**: ~0.31 GB

**显存节省**:
- 参数显存: 7.6 GB → 0.052 GB (节省 **7.55 GB**)
- 优化器显存: 30.4 GB → 0.208 GB (节省 **30.2 GB**)
- 梯度显存: 7.6 GB → 0.052 GB (节省 **7.55 GB**)
- **总节省**: ~**45.3 GB** (约99.3%的LLM相关显存)

### 2.3 完整模型显存对比

**46.2亿参数模型，使用LoRA后**:

| 组件 | 全量微调 | LoRA微调 | 节省 |
|------|---------|---------|------|
| LLM参数 | 7.6 GB | 0.052 GB | 7.55 GB |
| LLM优化器 | 30.4 GB | 0.208 GB | 30.2 GB |
| LLM梯度 | 7.6 GB | 0.052 GB | 7.55 GB |
| 其他模块 | ~8.5 GB | ~8.5 GB | 0 GB |
| 激活值 | ~10 GB | ~10 GB | 0 GB |
| **总计** | **~65.1 GB** | **~19.8 GB** | **~45.3 GB** |

## 三、使用LoRA后的GPU需求

### 3.1 DeepSpeed ZeRO-2 + LoRA

**每张卡显存需求** (4卡):
- 模型参数: (9.24 - 7.55) / 4 + 0.052 = 0.47 GB
- 优化器状态: (36.96 - 30.2) / 4 + 0.208/4 = 1.69 + 0.052 = 1.74 GB
- 梯度: (9.24 - 7.55) / 4 + 0.052/4 = 0.47 GB
- 激活值: ~10 GB
- **总计**: ~12.7 GB ✅ **单张A40即可！**

### 3.2 DeepSpeed ZeRO-3 + LoRA

**每张卡显存需求** (2卡):
- 模型参数: (9.24 - 7.55) / 2 + 0.052/2 = 0.87 GB
- 优化器状态: (36.96 - 30.2) / 2 + 0.208/2 = 3.48 GB
- 梯度: (9.24 - 7.55) / 2 + 0.052/2 = 0.87 GB
- 激活值: ~10 GB
- **总计**: ~15.2 GB ✅ **2张A40即可！**

## 四、LoRA配置示例

### 4.1 配置文件修改

在配置文件中添加LoRA配置：

```python
from peft import LoraConfig

# LoRA配置
llm_lora_config = dict(
    type=LoraConfig,
    r=16,  # LoRA rank，可以调整 (8, 16, 32, 64)
    lora_alpha=32,  # LoRA alpha，通常是r的2倍
    target_modules=[
        "self_attn.qkv_proj",  # Phi-3的attention层
        "self_attn.o_proj",
        "mlp.gate_up_proj",    # Phi-3的MLP层
        "mlp.down_proj",
    ],
    lora_dropout=0.05,
    bias="none",
    task_type="CAUSAL_LM",
)

# 在model配置中添加
model = dict(
    type=XSamModel,
    freeze_llm=False,  # 不冻结，但使用LoRA
    llm_lora=llm_lora_config,  # 添加LoRA配置
    # ... 其他配置
)
```

### 4.2 不同LoRA rank的参数量

| LoRA rank | LoRA参数量 | 显存占用 | 性能影响 |
|-----------|-----------|---------|---------|
| r=8 | ~13M | 最小 | 可能略低 |
| r=16 | ~26M | 小 | **推荐** |
| r=32 | ~52M | 中等 | 更好 |
| r=64 | ~104M | 较大 | 接近全量 |

**推荐**: r=16 或 r=32，平衡显存和性能

## 五、总结

### 5.1 显存节省

✅ **使用LoRA后，LLM相关显存从45.6GB降至0.31GB**
✅ **总显存需求从~65GB降至~20GB**
✅ **2-4张A40即可训练，甚至单卡也可能可行**

### 5.2 性能影响

- **优点**: 
  - 显存大幅节省
  - 训练速度快
  - 不易过拟合
  - 可以快速实验不同配置
  
- **缺点**:
  - 表达能力可能略低于全量微调
  - 需要选择合适的rank值

### 5.3 建议

**强烈建议使用LoRA**，特别是对于第三阶段训练：
1. 显存节省巨大（~45GB）
2. 训练效率高
3. 对于多任务微调，LoRA通常足够
4. 如果效果不够，可以增加rank值（r=32或64）

